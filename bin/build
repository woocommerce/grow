#! /bin/bash

source ~/.nvm/nvm.sh

case $1 in
	pnpm) echo 'pnpm'
		nvm use && pnpm install && pnpm run build
		;;
	npm) echo 'npm'
		nvm use && npm install && npm run build
		;;
	*) echo 'default'
		nvm use && $1
		;;
esac
